import protoTheme from './proto';

const variants = {
  buttons: {
    primary: {},
    secondary: {},
    accent: {},
  },
};

const theme = {
  ...protoTheme,
  ...variants,
};

export default theme;
